#include <stdio.h>
int main(){
    int bonus;
    int profit= 9999;
    int clients=20;
    if (profit > 1000)
    if (clients >15); //there will be no ; for if loop and also it's useless 
    
    bonus =100;
    printf("profit: %d, profit");
    return 0;
}
